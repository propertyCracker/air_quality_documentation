# Extraction
we extracted the data from the python code that delivers us the 
data stream each offset_seconds
# Transform
we transform the data into logs and standardize it for the llm for biasing 
# Load 
we load the data to our database